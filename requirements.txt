fastapi==0.115.6
uvicorn[standard]==0.32.1
aiogram==3.15.0
apscheduler==3.10.4
sqlmodel==0.0.22
psycopg[binary]==3.2.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
langgraph==0.2.60
langgraph-checkpoint-postgres==2.0.9
langchain-core==0.3.28
langchain-google-genai==2.0.7
python-dateutil==2.9.0.post0
tenacity==9.0.0
pyyaml==6.0.2
pytest==8.3.4
# Tuỳ chọn: sửa prompt trên UI + trace agent. Không cài thì code tự dùng prompt .md.
# Muốn có TRACE thì phải cài kèm gói `langchain`: langfuse.langchain.CallbackHandler
# import từ đó, langchain-core không đủ. Chốt <0.4 — bản 1.x mới là bản xung đột
# với langchain-core 0.3.x. Chỉ cần sửa prompt trên UI thì không cần langchain.
# langfuse>=3.0.0
# langchain>=0.3,<0.4
