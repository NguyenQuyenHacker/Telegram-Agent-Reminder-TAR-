fastapi==0.115.6
uvicorn[standard]==0.32.1
aiogram==3.15.0
apscheduler==3.10.4
sqlmodel==0.0.22
psycopg[binary]==3.2.3
# Kiểu VECTOR cho SQLAlchemy/SQLModel. Extension `vector` phải được bật sẵn ở
# phía Postgres (Neon): CREATE EXTENSION IF NOT EXISTS vector;
pgvector==0.5.0
python-dotenv==1.0.1
langgraph==0.2.60
langgraph-checkpoint-postgres==2.0.9
# Nới thay vì ghim ==0.3.28: môi trường đang chạy 0.3.86 và có sẵn `langchain`.
# Ghim cứng bản cũ là `pip install -r` HẠ CẤP core rồi kéo theo lỗi ở
# langchain-text-splitters. Chặn <0.4 vì 0.4 mới là bản phá tương thích.
langchain-core>=0.3.28,<0.4
# Kèm luôn GoogleGenerativeAIEmbeddings -> không cần thư viện embedding riêng.
# 2.0.7 KHÔNG có field `thinking_budget` (thêm ở 2.1.4, xem models.yaml khối
# client/sql/rewrite) và còn đi qua `google-generativeai` — gói đã deprecated,
# cảnh báo FutureWarning mỗi lần import. 2.1.4 chuyển sang gọi thẳng
# `google-ai-generativelanguage`, không còn phụ thuộc gói deprecated đó nữa.
langchain-google-genai==2.1.4
# EnsembleRetriever (hybrid BM25 + vector) nằm ở gói `langchain` chứ không phải
# langchain-core. tar_env TÌNH CỜ có sẵn 0.3.30 nên máy dev chạy được mà không
# khai — Docker build sạch sẽ nổ ImportError ngay lúc dựng graph client.
langchain>=0.3.14,<0.4
# Document loader: TextLoader (.txt). Bản .xlsx KHÔNG còn đi qua gói này nữa —
# `unstructured[xlsx]` đã bỏ hẳn, loaders.py đọc thẳng bằng openpyxl. Nhờ vậy
# không còn phụ thuộc `pandas` / `libmagic`, hai thứ chưa bao giờ kiểm chứng
# được trên Windows.
langchain-community>=0.3.14,<0.4
# BM25Retriever.from_texts() import gói này BÊN TRONG hàm, nên thiếu nó thì
# import module vẫn chạy và chỉ nổ lúc tra cứu lần đầu. CHƯA CÀI ở tar_env.
rank_bm25==0.2.2
# RecursiveCharacterTextSplitter
langchain-text-splitters>=0.3.5,<0.4
python-dateutil==2.9.0.post0
tenacity==9.0.0
# openpyxl: engine đọc .xlsx DUY NHẤT — loaders.read_workbook mở workbook một
# lần cho cả hai đầu ra (Document cho RAG, lưới ô cho extract).
openpyxl==3.1.5
# Đọc TAR_agent/utils/models.yaml. 6.0.2 không có wheel cho Python 3.14.
pyyaml==6.0.3
# Windows và image Docker slim KHÔNG có sẵn cơ sở dữ liệu múi giờ IANA, thiếu gói
# này là ZoneInfo("Asia/Ho_Chi_Minh") ném ZoneInfoNotFoundError ngay lúc import.
tzdata==2026.3
pytest==8.3.4
