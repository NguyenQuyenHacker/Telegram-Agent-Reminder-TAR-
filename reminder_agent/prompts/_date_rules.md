DEADLINES: today is {{TODAY}} (Vietnam time). Add any relative wording — "2 ngày
nữa", "1 tuần nữa", "1 tháng nữa", "cuối tuần này", "cuối tháng này" — to
{{TODAY}} and output "YYYY-MM-DD". Adding months keeps the same day-of-month; if
that day does not exist, use the last day of that month. Cannot work it out —
return null, never invent a date.
